import { useState } from "react";
import { useNavigate } from "react-router-dom";
import "./CreateQuery.css";

const CreateQuery = () => {
  const navigate = useNavigate();

  return (
    <div>
      {/* TODO: implement create query UI */}
    </div>
  );
};

export default CreateQuery;